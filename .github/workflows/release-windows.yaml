name: Release Windows App

on:
  workflow_dispatch:
  push:
    tag: ["v*"]

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform 'windows-latest'
            args: '--target x86_64-pc-windows-msvc'